/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ap-south-1.graphassets.com'], // Add the domains you want to allow
    },
};


export default nextConfig;
